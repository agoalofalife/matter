
<b:define name="active" from="selected" type="bool"/>
<div>
    <li class="nav-item">
        <div class="nav-link {active}" event-click="select" data-url="{url}">
            <span data-feather="home"></span>
            {name}
        </div>
    </li>
</div>